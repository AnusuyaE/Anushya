#include<stdio.h>
#include<string.h>
int main()
{
    char a[10];
    int b[10],i;
    scanf("%s",&a[i]);
    for(i=0;a[i]!='\0';i++)
    {
        if(a[i]>=0&&a[i]<=9)
        {
            printf("%d",b[i]);
        }
    }
    return 0;
    
}
